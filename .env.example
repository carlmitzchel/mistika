# Database (Neon Postgres — add via Vercel Marketplace, then vercel env pull)
DATABASE_URL=

# Vercel Blob (add BLOB_READ_WRITE_TOKEN via Vercel dashboard)
BLOB_READ_WRITE_TOKEN=

# Clerk (add via: vercel integration add clerk, then vercel env pull)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
